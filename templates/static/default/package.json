{
  "name": "[NAME]",
  "version": "[VERSION]",
  "description": "[DESCRIPTION]",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "jake default",
    "build": "jake build"
  },
  "author": "[AUTHOR]",
  "devDependencies": {
    "chokidar-cli": "^1.2.0",
    "http-server": "^0.9.0",
    "livereload": "^0.6.0",
    "pug-cli": "github:pugjs/pug-cli#master",
    "jake": "^8.0.15"
  }
}
